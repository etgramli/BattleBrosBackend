package de.etgramli.battlebros.model.card.effect;

public final class ReturnCardEffect extends CardEffect {
    public ReturnCardEffect(String effectText, EffectApplication target) {
        super(effectText, target, true);
    }
}
